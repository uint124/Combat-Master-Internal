#pragma once

namespace Il2cpp
{
	uint64_t GetRootDomain()
	{
		//auto get_domain_address = find_sig("", _("48 83 EC ? 48 8B 05 ? ? ? ? 48 85 C0 75 ? 33 D2"), text_section_hash); il2cpp::get_domain = decltype(il2cpp::get_domain)(get_domain_address);
		return 0;
	}
}